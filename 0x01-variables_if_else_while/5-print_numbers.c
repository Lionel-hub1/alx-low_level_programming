#include <stdio.h>
/**
 * main - Print all single numbers in base 10
 * Return: returns 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
