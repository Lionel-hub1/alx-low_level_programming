#include "main.h"
/**
 * add - adds two integers and returns the result
 * @num_one: first integer
 * @num_two: second integer
 * Return: sum of num_one and num_two
 */
int add(int num_one, int num_two)
{
	int sum;

	sum = num_one + num_two;
	return (sum);
}

